<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Product Details</title>
</head>

<body>
   <h1>
      <%= category %> Products
   </h1>
   <ul>
      <% for (let product of products){ %>
         <li><a href="/product/<%=product._id%>">
               <%=product.name %>
            </a>
            <a href="product/<%= product._id %>">details</a>
            <% } %>
         </li>
   </ul>
   <a href="/products/new">Crete New Product ?</a>
   &NonBreakingSpace;
   <% if(category !=='All' ){ %>
      <a href="/product">All Product</a>
      <% } %>
</body>

</html>